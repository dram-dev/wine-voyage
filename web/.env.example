# Root of the Wine Voyage API (no trailing slash, no /api suffix).
# Leave unset for local dev — vite.config.js proxies /api to localhost:8420.
# Example for a Tailscale-hosted backend:
# VITE_API_BASE=http://mac-mini.tail-xxxx.ts.net:8420
VITE_API_BASE=
