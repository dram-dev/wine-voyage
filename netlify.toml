[build]
  base = "web"
  command = "npm run build"
  publish = "dist"

# SPA: the app is a single client-rendered page, so every path serves index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
